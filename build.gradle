rootProject.version = "10.0.alpha17"
group = 'javasabr.rlib'

allprojects {
  repositories {
    mavenCentral()
  }
}

wrapper {
  gradleVersion = '9.1.0'
  distributionType = Wrapper.DistributionType.ALL
}
